export default function AnimeLayout({
  children,
  // modal,
}: {
  children: React.ReactNode;
  // modal: React.ReactNode;
}) {
  return (
    <>
      {children}
      {/* {modal} */}
    </>
  );
}
